<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_logo_smartchef"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_logo_smartchef"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartChef">

        <!-- FileProvider for secure APK package installation -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Splash Activity (Launcher) -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.SmartChef.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Container Activity -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustPan" />

        <!-- Recipe Results Activity -->
        <activity
            android:name=".activities.RecipeResultsActivity"
            android:exported="false" />

        <!-- Recipe Detail Activity -->
        <activity
            android:name=".activities.RecipeDetailActivity"
            android:exported="false" />

        <!-- Cooking Mode Activity -->
        <activity
            android:name=".activities.CookingModeActivity"
            android:exported="false"
            android:theme="@style/Theme.SmartChef.Cooking" />
    </application>
</manifest>
